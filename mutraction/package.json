{
  "name": "mutraction",
  "version": "0.2.0",
  "description": "Tired of spreads? Just mutate. Transact. Commit. Roll back. Undo. Redo.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "bundle": "npm run clean && npx tsc",
    "test": "npx tsc && npx uvu dist/tests",
    "clean": "del /Q /S dist"
  },
  "author": "Tom Theisen",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.18.15",
    "typescript": "^5.1.6",
    "uvu": "^0.5.6"
  }
}
