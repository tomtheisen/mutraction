{
  "name": "mutraction",
  "version": "0.2.0",
  "description": "Tired of spreads? Just mutate. Transact. Commit. Roll back. Undo. Redo.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "bundle": "npm run clean && npx tsc && esbuild ./out/index.js --bundle --outfile=./dist/index.js && npx tsc -p tsconfig-declarations.json",
    "test": "npx tsc && npx uvu out/tests",
    "clean": "rmdir /Q /S dist && rmdir /Q /S out"
  },
  "author": "Tom Theisen",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.18.15",
    "typescript": "^5.1.6",
    "uvu": "^0.5.6"
  }
}
